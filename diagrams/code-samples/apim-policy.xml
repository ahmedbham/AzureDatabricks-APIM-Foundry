<policies>
    <inbound>
        <base />
        <!-- 1. Validate the delegated caller token before overwriting Authorization for the backend hop -->
        <!-- Require both the delegated scope and an app role that is assigned to the allowed Entra groups -->
        <validate-jwt header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized" require-scheme="Bearer" output-token-variable-name="jwt">
            <openid-config url="https://login.microsoftonline.com/{TENANT_ID}/v2.0/.well-known/openid-configuration" />
            <audiences>
                <audience>{APIM_API_APP_ID}</audience>
            </audiences>
            <issuers>
                <issuer>https://login.microsoftonline.com/{TENANT_ID}/v2.0</issuer>
            </issuers>
            <required-claims>
                <claim name="scp" match="all">
                    <value>user_impersonation</value>
                </claim>
                <claim name="roles" match="any">
                    <value>{DELEGATED_GROUP_APP_ROLE_VALUE}</value>
                </claim>
            </required-claims>
        </validate-jwt>
        <!-- 2. Acquire token for the Managed Identity -->
        <!-- Use "https://cognitiveservices.azure.com/" for Azure OpenAI models -->
        <authentication-managed-identity resource="https://cognitiveservices.azure.com/" output-token-variable-name="msi-access-token" ignore-error="false" />
        <!-- 3. Set the Authorization header for the backend -->
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + (string)context.Variables["msi-access-token"])</value>
        </set-header>
        <set-backend-service id="apim-generated-policy" backend-id="aif-foundry-ai-endpoint" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>